# Copy this file to .env and fill in your own values.
# NEVER commit the real .env file or a real API key to source control.

# Get a fresh key from https://aistudio.google.com/app/apikey
# (revoke the old key that was previously hardcoded in movieRecommender.py)
GEMINI_API_KEY=

# Optional
PORT=5000
FLASK_DEBUG=0
